# Public URL of the Imix backend API.
# Nginx will proxy /api to this upstream.
API_UPSTREAM=https://api.example.com

# Host port to expose the SPA on (Dokploy usually routes via Traefik instead).
LANDING_PORT=8082
